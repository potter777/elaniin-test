let pageHome = new class {
    /*
    infoPhoto: HTMLDivElement;

    constructor() {
        this.prepareInfoPhotoElement();
        //añadimos el evento onmouse a las imágenes de la galleria
        let cards:Array<HTMLElement> = document.querySelectorAll('.works--card');
        for (let card of cards) {
            card.addEventListener('mouseover', this.onMouseOver_card);
        }
    }

    onMouseOver_card(event: Event) {
        console.log(this);
    }

    createInfoPhotoElement() {
        this.infoPhoto = document.createElement('div');
        let captionMake = document.createElement('p');
        captionMake.className='caption';
        captionMake.textContent = 'MAKE';
        let captionModel = document.createElement('p');
        captionModel.className='caption';
        captionModel.textContent = 'MODEL';
        let captionShutter = document.createElement('p');
        captionShutter.className='caption';
        captionShutter.textContent = 'SHUTTER SPEED';
        let captionAperture = document.createElement('p');
        captionAperture.className='caption';
        captionAperture.textContent = 'APERTURE';
        let captionFocal = document.createElement('p');
        captionFocal.className='caption';
        captionFocal.textContent = 'FOCAL LENGTH';
        let captionISO = document.createElement('p');
        captionISO.className='caption';
        captionISO.textContent = 'ISO';
    }
    */
}
